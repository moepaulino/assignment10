# Module 10
DockerHub URL: https://hub.docker.com/r/leopaulino/601_module10
# Build Docker Image
```docker build -t <image-name> .```
# Run Docker Container
```docker run -it --rm <image-name>```
# Run Tests Locally
```pytest```